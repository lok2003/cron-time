name: Schedule CI
on:
  push:
    branches:
      - main
  #schedule:
    #- cron: '*/5 * * * *'
jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - name: checkout code 
          uses: actions/checkout@v3
        - name: Run test
          run: echo "Running Scheduled job"